const WaitingFor = () => {
    return (
        <div className="home__waiting">
            <h2 className="waiting__header">What are you waiting for?</h2>
            <button className="waiting__button">Order Now!</button>
        </div>
    );
};

export default WaitingFor;
